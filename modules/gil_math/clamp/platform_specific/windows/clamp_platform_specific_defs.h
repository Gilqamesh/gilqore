#ifndef CLAMP_WINDOWS_DEFS_H
# define CLAMP_WINDOWS_DEFS_H

# include "../../clamp_defs.h"

enum clamp_windows_error_code {
    WINDOWS_CLAMP_ERROR_CODE_START,
};


enum clamp_windows_error_code {
    WINDOWS_CLAMP_ERROR_CODE_START,

};

#endif
