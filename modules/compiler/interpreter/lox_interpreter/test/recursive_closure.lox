fun create_factorial() {
  fun factorial(n) {
    if (n <= 1) return 1;
    return n * factorial(n - 1);
  }
  return factorial;
}

var factorial = create_factorial();

var num = 5;
var result = factorial(num);
print "Factorial of " + num + " is: " + result;